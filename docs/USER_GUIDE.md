# CH34X Android USB Tool 用户指南

## 📱 应用概述

CH34X Android USB Tool 是一款专业的安卓USB主机模式工具，支持CH340E和CH341B芯片的UART通信和SPI Flash编程。本应用提供完整的工业级功能，包括自动设备识别、错误恢复、数据校验和进度显示。

## 🚀 快速开始

### 1. 安装应用
- 从GitHub Actions下载最新的APK文件
- 在安卓设备上安装APK（需要允许未知来源安装）
- 确保设备支持USB Host模式（OTG）

### 2. 硬件连接
1. 准备CH340E或CH341B USB设备
2. 准备OTG线（如果手机使用USB-C接口）
3. 将CH34X设备通过OTG线连接到手机
4. 应用会自动检测设备

### 3. 权限请求
首次运行时，应用会请求以下权限：
- **USB权限**: 允许应用访问USB设备
- **存储权限**: 用于保存日志和读取文件

## 🎯 主界面

主界面包含以下主要部分：

### 状态栏
- **状态指示灯**: 显示当前连接状态
  - 🟢 绿色: 已连接
  - ⚪ 灰色: 未连接
  - 🟡 黄色: 连接中
  - 🔴 红色: 错误

- **当前设备信息**: 显示已连接设备的基本信息

### 设备列表
- 自动扫描并显示所有已连接的CH34X设备
- 点击设备卡片进行连接
- 设备状态指示灯显示连接状态

### 功能按钮
- **UART调试**: 进入串口通信模式
- **SPI通信**: 进入SPI通信模式
- **Flash编程**: 进入Flash烧录模式

## 📡 UART模式

### 配置参数

#### 波特率设置
- 使用滑块调整波特率（1200-3000000）
- 常用波特率：9600, 19200, 38400, 115200, 921600

#### 高级设置
- **数据位**: 5, 6, 7, 8
- **停止位**: 1, 1.5, 2
- **校验位**: 无校验、奇校验、偶校验、标记校验、空格校验
- **流控制**: 无流控、硬件流控、软件流控

### 数据收发

#### 发送数据
1. 在输入框中输入要发送的文本
2. 点击"发送"按钮
3. 数据会实时显示在接收区域

#### 接收数据
- 接收到的数据实时显示在屏幕上
- 支持自动滚动到底部
- 可选择十六进制或ASCII显示模式

### 日志功能

#### 开始日志记录
1. 点击"开始日志"按钮
2. 选择保存位置
3. 所有收发数据都会被记录

#### 日志格式
[HH:mm:ss.SSS] TX -> [5 bytes]
HEX: 48 65 6C 6C 6F
ASC: Hello

[HH:mm:ss.SSS] RX <- [5 bytes]
HEX: 57 6F 72 6C 64
ASC: World

## 🔧 SPI模式

### SPI配置

#### SPI模式选择
- **模式0**: CPOL=0, CPHA=0
- **模式1**: CPOL=0, CPHA=1
- **模式2**: CPOL=1, CPHA=0
- **模式3**: CPOL=1, CPHA=1

#### 时钟速度
- 可设置SPI时钟频率（Hz）
- 常用速度：1MHz, 5MHz, 10MHz

#### 数据顺序
- **MSB优先**: 默认模式
- **LSB优先**: 最低有效位优先

### 数据传输

#### 基本传输
1. 输入要发送的数据（十六进制或ASCII）
2. 点击"传输"按钮
3. 接收数据实时显示

#### 读取操作
1. 设置读取长度（字节数）
2. 点击"读取"按钮
3. 读取结果显示

#### 写入操作
1. 输入要写入的数据
2. 点击"写入"按钮
3. 确认写入成功

## 💾 Flash编程模式

### Flash选择

#### 从数据库选择
- 内置常用Flash芯片数据库
- 支持搜索和筛选
- 显示芯片详细信息

#### 自动识别
1. 连接Flash芯片
2. 点击"识别Flash"按钮
3. 应用自动识别型号

### Flash信息显示
- **型号**: 芯片型号
- **容量**: 芯片容量（字节/MB）
- **页大小**: 编程页大小
- **扇区大小**: 擦除扇区大小
- **Flash ID**: 制造商ID和设备ID

### 基本操作

#### 读取Flash
1. 设置起始地址（十六进制）
2. 设置读取长度（字节）
3. 点击"读取"按钮
4. 选择保存位置
5. 等待完成，显示进度

#### 写入Flash
1. 选择要写入的文件
2. 设置起始地址
3. 选择选项：
   - ✅ 写入后校验
   - ✅ 自动擦除
4. 点击"写入"按钮
5. 等待完成，显示进度

#### 擦除Flash
1. 设置起始地址
2. 设置擦除长度
3. 点击"擦除"按钮
4. 等待完成

#### 整片擦除
- 点击"整片擦除"按钮
- 确认操作
- 等待完成（可能需要较长时间）

### 校验功能
- 自动CRC32校验
- 写入后可选自动校验
- 校验失败自动重试

### 数据库管理

#### 导出数据库
1. 点击"导出数据库"按钮
2. 选择保存位置
3. 数据库以JSON格式保存

#### 导入数据库
1. 点击"导入数据库"按钮
2. 选择JSON文件
3. 自动合并到现有数据库

## ⚙️ 高级功能

### 自动重试机制
- 通信失败自动重试（最多3次）
- 可配置重试次数
- 错误日志记录

### 数据对齐
- 自动页对齐处理
- 自动块对齐处理
- 智能偏移计算

### 错误恢复
- 通信超时处理
- 设备断开自动检测
- 自动重连机制

## 📊 状态监控

### 进度显示
- 实时进度条
- 完成百分比
- 当前速度（字节/秒）

### 状态指示灯
- **连接状态**: 显示设备连接状态
- **操作状态**: 显示当前操作状态
- **错误状态**: 显示错误状态

### 日志查看
- 实时显示操作日志
- 错误信息提示
- 成功操作确认

## 🔐 权限说明

### 必需权限
- **USB权限**: 用于访问USB设备
- **存储权限**: 用于读写文件

### 可选权限
- **通知权限**: 用于后台操作通知

## 🐛 故障排除

### 常见问题

#### 设备无法识别
1. 检查OTG线是否正常工作
2. 确认设备供电充足
3. 尝试重新插拔设备
4. 检查设备驱动是否安装

#### 通信失败
1. 检查连接状态指示灯
2. 确认参数配置正确
3. 尝试降低通信速度
4. 查看错误日志

#### 写入校验失败
1. 检查电源供应
2. 确认芯片型号正确
3. 尝试降低写入速度
4. 检查连接线质量

### 日志分析
- 查看错误日志定位问题
- 导出日志进行分析
- 记录问题发生时的操作

## 📝 最佳实践

### UART通信
- 使用适当的波特率
- 确保双方参数一致
- 使用流控避免数据丢失

### SPI编程
- 先读取验证芯片ID
- 写入前确认容量
- 重要数据先备份
- 使用校验功能

### 文件管理
- 定期备份数据库
- 重要固件多处备份
- 使用有意义的文件名

## 🔄 版本更新

### 检查更新
- GitHub Actions自动构建
- 关注Release页面
- 及时更新获取新功能

### 更新日志
- 查看CHANGELOG了解更新
- 关注新功能和修复
- 注意兼容性变更

## 📞 技术支持

### 问题反馈
- GitHub Issues: 报告bug
- 提供详细的错误描述
- 附上相关日志

### 功能建议
- 通过GitHub提交建议
- 描述使用场景
- 提供参考实现

---

**版本**: 1.0.0  
**最后更新**: 2024-01-15  
**兼容性**: Android 10-13 (API 29+)
